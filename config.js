// Supabase connection settings.
// ---------------------------------------------------------------------------
// Fill these in with the values from your Supabase project:
//   Supabase Dashboard -> Project Settings -> API
//     * Project URL          -> SUPABASE_URL
//     * Project API keys: anon public -> SUPABASE_ANON_KEY
//
// The anon (public) key is SAFE to expose in the browser. Access is controlled
// by the Row Level Security policies in supabase-schema.sql, NOT by hiding this
// key. Never put the `service_role` key here.
// ---------------------------------------------------------------------------

window.SUPABASE_URL = "YOUR_SUPABASE_PROJECT_URL";
window.SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
